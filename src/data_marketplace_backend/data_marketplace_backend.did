import principal "mo:base/Principal";

service : {
  upload_dataset: (text, text, nat64, text) -> (nat64);
  get_all_datasets: () -> (vec record { nat64; text; nat64; text; principal });
  request_access: (nat64) -> (text);
  get_pending_requests: (nat64) -> (vec principal);
  approve_buyer: (nat64, principal) -> (text);
  view_dataset: (nat64) -> (text);
};

